package weapons.spanishrapier;

import combat.defs.*;
import weapons.Weapon;

public class SpanishRapier extends Weapon {
 
    public SpanishRapier(Stance stance) {
        super(stance);
    }

    public void updateAttacks(Stance stance) {
        switch(stance) {
            case HIGH_INNER:

            break;

            case HIGH_OUTER:

            break;

            case LOW_INNER:

            break;

            case LOW_OUTER:

            break;
        }
    }

}
